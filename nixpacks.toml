[phases.setup]
nixPkgs = ['python310', 'pip', 'ffmpeg', 'git']

[phases.install]
cmds = [
  'pip install --upgrade pip',
  'pip install --no-cache-dir -r requirements.txt'
]

[start]
cmd = 'uvicorn server:app --host 0.0.0.0 --port $PORT'

